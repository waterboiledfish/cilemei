适合约 6 分钟答辩，按顺序或抽问使用。每题建议控制在 30 秒～1 分钟内。

---

## 1. 这个项目是做什么的？解决了什么问题？

**答：**「吃了吗」是一个面向个人的健康饮食与生活方式辅助网站。用户注册登录后可以填写健康档案（身高、体重、疾病史、过敏史、生活习惯），通过两种方式获得 AI 建议：一是仅根据文字健康信息生成饮食与生活建议，二是上传/拍摄食物照片，由 AI 识别食物并结合健康档案给出针对性建议。解决的是普通人没有专业营养师时，仍能获得个性化、可执行的饮食与生活习惯指导，并支持「拍一下当前吃的」这种贴近日常的场景。

---

## 2. 目标用户是谁？使用场景是什么？

**答：** 目标用户是关注饮食健康、但没有固定营养顾问的普通用户。典型场景包括：想控制体重或改善体质的人、有慢性病或过敏需要忌口的人、经常外卖/外食想了解营养结构的人。用户可以在家、办公室或餐厅随时录入健康信息或拍下当前食物，通过浏览器快速得到建议，无需安装复杂 App。

---

## 3. 前后端是如何分离的？为什么这样设计？

**答：** 前端是纯 HTML/CSS/JS 单页应用，放在 `public/`，通过 Fetch 调用后端 REST API；后端是 Node.js + Express，提供注册、登录、健康档案和 AI 分析等接口。本地开发时 `server.js` 同时托管静态资源和 API；部署到 Vercel 时，静态资源由 Vercel 提供，API 通过 `api/[[...path]].js` 以 Serverless 形式运行。这样既能在本地一键跑全栈，又能在 Vercel 上低成本部署，前后端职责清晰、便于维护和扩展。

---

## 4. 数据库有几张表？分别存什么？为什么这样设计？

**答：** 有三张表：`users` 存用户（id、email 唯一、password_hash、name）；`health_profiles` 存健康档案（身高、体重、疾病史、过敏史、生活习惯等），按用户、按时间保留多条，查询时取最新一条；`advice_history` 存每次生成的文字建议或拍照识别建议的完整内容，方便用户查看建议历史并点击某条重新查看。密码只存 bcrypt 哈希不明文，email 唯一保证一邮箱一账号。

---

## 5. 登录状态是如何保持的？会不会被伪造？

**答：** 登录成功后后端签发 JWT（含用户 id、email，有效期 7 天），前端把 token 存到 localStorage，之后每次请求在 Header 里带 `Authorization: Bearer <token>`。后端在需要鉴权的接口里用 `JWT_SECRET` 校验签名，非法或过期的 token 会返回 401。密钥只在服务端，无法在不知道密钥的情况下伪造有效 token。

---

## 6. AI 建议是怎么实现的？用的是哪家模型？

**答：** 后端根据用户健康档案（或结合识别到的食物）拼成 prompt，通过 HTTP 调用阿里云 DashScope 的 OpenAI 兼容接口，使用 qwen-vl-plus 模型。文字建议走 `/api/analyze`，返回完整文本；拍照识别走 `/api/food-analyze`，请求里带 base64 图片，模型返回 JSON（foods、nutrition_notes、advice、cautions），后端解析并格式化成易读中文后流式输出给前端。

---

## 7. 为什么页面上是「一段一段」出字，而不是等全部生成完再显示？

**答：** 为了更好的体验，我们采用流式输出：后端拿到 AI 完整回复后，按段落拆成小块，每隔约 120ms 写一块到响应体，前端用 `fetch` 的 `response.body.getReader()` 读流，每读到一块就追加到页面并滚动到底部。这样用户能尽快看到首屏内容。在 Vercel 10 秒限制下，文字建议约 9 秒超时，拍照识别视觉接口 9.5 秒超时，且前端上传前会压缩图片、后端 prompt 精简，超时后返回明确提示，避免一直卡住。

---

## 8. 为什么选择 Vercel + Supabase？

**答：** 主要考虑零运维、免费额度够用、快速上线。Vercel 提供免费 HTTPS 和自动部署，Supabase 提供免费 Postgres 和连接池，不需要自己买服务器、配 Nginx、管数据库。项目体量适合 Serverless + 云数据库；以后用户量大了再迁移到自建或更重的云服务也来得及。

---

## 9. 拍照识别时，模型返回的 JSON 不规范怎么办？

**答：** 我们做了容错：先剥掉可能的 markdown 代码块再 `JSON.parse`；解析成功后统一成规范字段，支持中英 key（如 foods/食物、advice/建议），cautions 只保留一条非空字符串避免刷屏；若解析失败，用正则从文本里提取 foods、nutrition_notes、advice、cautions，再格式化为易读文本展示，绝不把整段原始 JSON 直接给用户看。

---

## 10. 为什么有时候会「识别超时」？做了哪些优化？

**答：** Vercel 免费版函数有约 10 秒执行上限，视觉模型响应慢就容易超时。我们做了多重缓解：视觉接口设为 9.5 秒超时（可用环境变量 `AI_VISION_TIMEOUT_MS` 微调）；前端在上传前用 canvas 把图片压缩为最长边 1024px、JPEG 质量 0.82；后端 prompt 精简以减少 token。超时后返回「AI 视觉识别超时，请稍后重试或换一张图」。若仍常超时，可换更清晰简单的食物图或升级 Vercel 计划。

---

## 11. 建议历史是怎么实现的？数据存在哪里？

**答：** 每次生成文字建议或拍照识别建议成功后，后端在返回流式内容前先把完整文本插入 `advice_history` 表（user_id、type 为 text 或 food、content 为全文）。前端通过 `GET /api/advice-history?limit=20` 拉取当前用户的历史列表，在右侧「建议历史」展示；点击某条就把该条 content 回填到上方建议框重新查看。存储按条数增长，以后可加「只保留最近 N 条」或用户主动删除。

---

## 12. 这个项目的主要亮点和不足分别是什么？

**答：** 亮点：从注册、健康档案到文字+图像两种 AI 建议形成完整闭环；流式输出、图片缩放与拖拽、建议历史自动入库可回看；针对识别超时做了前端压缩图片、9.5s 超时、prompt 精简；AI 返回格式容错（剥离代码块、统一中英字段、cautions 只一条、解析失败时不展示原始 JSON）；技术选型兼顾开发效率和免费部署，密码与鉴权、环境变量有基本安全考虑。不足：免费版 Vercel 10 秒限制下个别场景仍可能超时；历史暂无分页或删除单条；若误提交过 `.env` 需轮换密钥。后续可以针对这些点迭代。

---

## 13. 你们团队是怎么分工的？你个人负责哪几块？

**答：**（可按实际调整）我们按五人分工：前端负责页面、样式、交互、流式展示和上传前图片压缩；后端负责服务端逻辑、鉴权、数据库和所有 API；AI 对接负责调用 DashScope、设计 prompt、解析与容错、超时设置；部署负责 Vercel/Supabase 配置和排错；产品与文档负责需求、README、FAQ 和答辩材料。我本人负责整体架构、后端 API 与数据库设计、Vercel/Supabase 接入以及前后端联调。

---

## 14. 整个项目里你遇到的最大难点是什么？怎么解决的？

**答：** 主要有几个难点：（1）Vercel 上 Express 不能直接作为根入口，需要改成 Serverless 入口 `api/[[...path]].js` 再共用 Express 路由；（2）Supabase 直连在 Vercel 下 IPv4 兼容问题，需改用 pooler URI（端口 6543）；（3）模型有时返回非标准 JSON，需要剥离代码块、统一中英 key、cautions 不重复、解析失败时正则抽取；（4）拍照识别在 10 秒限制下易超时，通过前端压缩图片、视觉 9.5s 超时、精简 prompt 缓解。通过查文档、看报错日志和逐步缩小范围排查解决。

---

## 15. 和第一版相比改动很大，是不是重做了？怎么解释？

**答：** 不是推倒重做，是在初版基础上的迭代与完善。功能上第一稿侧重「能不能跑通」，当前版明确了三点：用户数据要落库并支持多端、AI 结果要流式呈现、拍照识别要支持放大缩小和回看历史。技术上第一稿可能是纯前端或简单后端，当前版是「前端 + 鉴权后端 + 云数据库 + 外部 AI API」的完整应用，为统一技术栈和便于部署做了架构与 UI 重构，但核心目标（健康档案 + AI 建议 + 拍照识别）一致。

---

## 16. 如果以后要支持「多设备登录」或「记住我」，怎么扩展？

**答：** 当前 JWT 是无状态的，同一 token 在有效期内可多设备使用。若要做「记住我」，可签发更长时间的 token 或使用 refresh token。若要做设备管理（列出设备、远程登出），可在库中增加 `user_sessions` 表记录 token 或 device_id，登出或改密时使对应 token 失效，校验时除验证 JWT 签名外再查一次库。

---
